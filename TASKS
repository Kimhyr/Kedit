[ ] Error system.
	- Simply just throw a element from some `ErrorCode` enum.

[ ] Search up how are decision trees are represented in discrete mathematics.
[ ] Make a Reddit post on r/Vent about how I got a 96 on the EOC with failing grades.
[ ] Create a wrapper for `stdio.h`.

* Keybinds should be modal.
	- Keybinds aren't based off their operation (e.g. `b` for "back");
	  keybinds are based off how good the location is.
	- CAPS is the mode switcher.
		* insert and
		* normal mode.
	- Keymaps:
		* BitMovement:
			- J - Move down
			- K - Move up
			- L - Move right
			- ; - Move left
		* ByteMovement (move per token/line):
			- CTRL + [BitMovement]
		* SelectMovement ():
			- SHIFT + [BitMovement|BlockMovement]

